import React from 'react';

const ContentChoice = ({choice}) => {
    return (
        <div>
        <h2 className="text-center text-2xl font-semibold">{choice} </h2>
        <p className="text-[#F6AB9E] text-lg text-center">xxx</p>
    </div>
    );
};

export default ContentChoice;